# Copy to .env.local and fill in your values. .env.local is gitignored.
# TMDB API Read Access Token (v4 auth), used for local testing to configure
# the app's Settings page. Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
